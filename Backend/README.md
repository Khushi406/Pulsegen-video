# Pulsegen Backend

Simple Express backend scaffold for video upload, processing and real-time updates.

Getting started

1. Install dependencies:

```bash
cd Backend
npm install
```

2. Create `.env` from `.env.example` and update values.

3. Run in development:

```bash
npm run dev
```
